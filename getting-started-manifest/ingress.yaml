apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: getting-started-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: my-load-balancer-140722552536.ap-northeast-1.elb.amazonaws.com
    http:
      paths:
      - path: /hello
        pathType: Prefix
        backend:
          service:
            name: getting-started-service
            port:
              number: 8080